<div class="row">
		<% @users.each do |u| %>
		<div class="col-md-8">
			<h4><%= link_to u.name, user_path(u) %></h4>
		</div>
		<div class="col-md-4">
			
		</div>
			<h4><%= u.rating %></h4>
		<% end %>
</div>