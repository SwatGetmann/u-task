<% @users.each do |u| %>
	<div class="row">
		<div class="col-md-8">
			<h4><%= link_to u.name, user_path(u) %></h4>
		</div>
		<div class="col-md-4">
			<h4><%= u.rating %></h4>
		</div>
	</div>
<% end %>

<%= t('.no-users-found') if @tasks.empty? %>
