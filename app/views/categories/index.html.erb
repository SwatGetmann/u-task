<h1><%= t('.categories') %></h1>

<div class="row">
  <% @categories.each do |category| %>
    <%= link_to category.name, category_path(category), class: "btn btn-default" %>
	<% end %>
</div>

<div class="row">
  <div class = "col-md-12">
    <br>
    <%= link_to t('.new-category'), new_category_path, class: "btn btn-success" %>
  </div>
</div>